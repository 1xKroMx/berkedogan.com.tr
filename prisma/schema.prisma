datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Task {
  id          String    @id @default(uuid())
  title       String
  category    String
  status      String
  created_at  DateTime  @default(now())
  due_date    DateTime?
  description String?
}
